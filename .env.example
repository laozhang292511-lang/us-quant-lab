# Copy to .env only on an approved personal machine. Never commit .env.
QUANT_MODE=backtest
QUANT_INSTALL_ROOT=D:\老张\量化交易

# Live trading stays disabled until a separate go-live review.
LIVE_TRADING_ENABLED=false
BROKER_NAME=schwab

# Add Schwab variables only on the future approved execution host.
# SCHWAB_API_KEY=
# SCHWAB_APP_SECRET=
# SCHWAB_CALLBACK_URL=https://127.0.0.1
# SCHWAB_TOKEN_PATH=D:\secure\schwab-token.json

# Optional LLM provider for Vibe-Trading. Prefer OAuth/keychain where supported.
# LANGCHAIN_PROVIDER=openai-codex
# LANGCHAIN_MODEL_NAME=
